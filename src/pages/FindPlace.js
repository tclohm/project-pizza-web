import React from "react";

export default function FindPlace() {
	let url = `https://maps.googleapis.com/maps/api/geocode/json?latlng=${latitude},${longitude}&key=${REACT_GG_KEY}`

	return <p>search page</p>
}