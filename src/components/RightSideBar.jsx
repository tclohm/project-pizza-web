const RightSideBar = () => {
	// Maybe we use context to hold the state for now
	// but we will have to send it to the server database
	// watched venue will have to 
	return (
		<section>
			<div>
				<h2 className="text-md font-bold">👂 Recently Viewed</h2>
				<div className="bg-gray-200 border rounded"></div>
			</div>
		</section>
	)
}

export default RightSideBar